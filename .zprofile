# Prompt
[[ -f ~/.zshrc ]] && . ~/.zshrc

# aliases
[[ -f ~/.bash_aliases ]] && . ~/.bash_aliases

# Prompt
[[ -f ~/.bash_prompt ]] && . ~/.bash_prompt

# Functions
[[ -f ~/.bash_functions ]] && . ~/.bash_functions
